spring.application.name=blistra-backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# User timezone used to interpret Planner calendar semantics (today/overdue).
# A per-user timezone will replace this default once a user profile exists.
planner.user-timezone=${PLANNER_USER_TIMEZONE:Asia/Kolkata}
